# Nome da aplicação
spring.application.name=demo
springdoc.swagger-ui.path=/swagger.html

spring.datasource.url=${SUPABASE_URL}
spring.datasource.username=${SUPABASE_USER}
spring.datasource.password=${SUPABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do JPA 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configurações de upload llllll
# file.upload-dir=${FILE_UPLOAD_DIR:${java.io.tmpdir}/app-uploads}
# file.upload-dir=${user.home}/app-uploads

# Configurações multipart do Spring
 spring.servlet.multipart.enabled=true
 spring.servlet.multipart.location=${file.upload-dir}
 spring.servlet.multipart.max-file-size=5MB
 spring.servlet.multipart.max-request-size=5MB
 spring.servlet.multipart.file-size-threshold=2MB

 # Configurações do H2
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Configuração do banco de dados H2 em memória
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.sql.init.platform=h2
# spring.sql.init.mode=always
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
